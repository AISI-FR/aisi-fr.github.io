source "https://rubygems.org"

# Thème local « aisi-blog » — Jekyll natif (compatible GitHub Pages, Jekyll >= 4.x)
gem "jekyll", "~> 4.4"
gem "rouge"                # coloration syntaxique serveur (highlighter du _config.yml)

# Requis par Ruby >= 3.0 pour `jekyll serve`
gem "webrick", "~> 1.8"

# Utilitaires optionnels
group :jekyll_plugins do
  # gem "jekyll-feed"
end
